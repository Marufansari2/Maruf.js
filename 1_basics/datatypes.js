"use strict"; // treat as new version

//alert("Maruf") // we are using node.js not browser,it is run in console

console.log(3+3) // we can also use semi colon there is not any affect on code
// we can also use like
console.log(3
    +3
) //it can be also run but not good for good code

let name ="maruf"
let age =20
let isLoggedIn=false
let state=null // object


console.log(typeof null);
